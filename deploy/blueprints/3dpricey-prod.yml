public-resources:
  3dpricey-prod:
    name: 3DPricey Production
    protocol: http
    full-domain: printel.ro
    targets:
      - hostname: localhost
        port: 3001
        method: http
        path: /api
        path-match: prefix
        priority: 10
      - hostname: localhost
        port: 8080
        method: http
        path: /
        path-match: prefix
        priority: 100
